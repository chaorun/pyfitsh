# 本次会话修改清单（未经审查的文件）

## 修改批次 1：`kernel_info_read_dicts` 新增

| 文件 | 变更 |
|------|------|
| `fiphot/fiphot_core.c` | 第 1734 行后新增 `kernel_info_read_dicts` 函数（~60 行），构建 kernellist 结构 |
| `fiphot/fiphot_core.c` | `fiphot_photometry_from_raw_cy` 签名新增 `int nkernel, int *k_types, int *k_orders, int *k_ncoeffs, int *k_hsizes, double *k_sigmas, double **k_coeffs, double k_ox, double k_oy, double k_scale` |
| `fiphot/fiphot_core.c` | kernel 解析块改为双路径（`nkernel>0` 走 dict 路径 / 否则走 `kernel_spec` 字符串路径） |
| `fiphot/fiphot_core.h` | 新增 `#include "kernel.h"` + `kernel_info_read_dicts` 声明 + 更新 `fiphot_photometry_from_raw_cy` 签名 |
| `fiphot/fiphot_cdef.pxd` | 更新 `fiphot_photometry_from_raw_cy` 签名 |
| `fitsh_cy.pyx` | `photometry_from_raw` 新增 `kernel_dict=None` 参数 + 核数据扁平化逻辑 + `kd_coeff_ptrs` malloc/free |

## 修改批次 2：bx/by basis 支持

| 文件 | 变更 |
|------|------|
| `ficonv/ficonv_core.c` | 第 830 行 kernel list 输出添加 `b=%d,%d` |
| `fitrans/fitrans_internals.pxi` | `_parse_kernel_list` 添加 `b=` 解析分支 → `kd['bx']` / `kd['by']` |
| `fitrans/fitrans_internals.pxi` | 临时添加 debug 打印代码后被撤销 |
| `fiphot/fiphot_core.c` | `kernel_info_read_dicts` 签名新增 `int *k_bx, int *k_by` |
| `fiphot/fiphot_core.c` | `fiphot_photometry_from_raw_cy` 签名新增 `int *k_bx, int *k_by` |
| `fiphot/fiphot_core.c` | kernel 结构体添加 `k->bx = k_bx ? k_bx[i] : 0; k->by = k_by ? k_by[i] : 0;` |
| `fiphot/fiphot_core.h` | 两处声明新增 `k_bx`/`k_by` |
| `fiphot/fiphot_cdef.pxd` | 声明新增 `k_bx`/`k_by` |
| `fitsh_cy.pyx` | 新增 `kd_bx_view`/`kd_by_view` 提取 + 传递 |

## 后果

- `.so` 被 `rm -rf build fitsh_cy.c fitsh_cy.cpython-312-darwin.so` 删除
- 重新编译失败：`wcs.pxi:122` 报 `'wcs_fit_input' is not a type identifier` — **预存问题，非本次修改导致**
- 编译未通过，无法验证 bx/by 修改是否生效
