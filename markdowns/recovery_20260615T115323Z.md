# 快照恢复: fitsh_cython_20260615T115323Z.zip

## 来源
- 构建于 session-ses_1353.md (6/15 18:09 - 21:41) 之前
- 被 session 1353 反复 extract 恢复

## 重建方法
1. 解压 `fitsh_cython-20260614T084708Z.zip` 作为基线
2. 应用 session-ses_13af.md 和 session-ses_13a6.md 中的改动（变量重命名、头文件路径调整等 Cython 移植工作）
3. 确保以下文件为 zip 原始状态（今日修复前）:

### setup.py
- `extra_compile_args=["-O0", "-g"]` （尚未改为 -O3）

### fistar/fistar_core.c
- 包含 `else switch (algorithm)` 原始格式（尚未做 sr 作用域修复）
- `sfp.iter_symmetric = 4; sfp.iter_general = 2;` 硬编码（尚未用函数参数）
- `if (stars == NULL && is_fit_model)` 原始回退逻辑
- dump 代码用 `/* ... */` 注释
- `double *cand_xy, int in_ncand, double cand_radius` 无 cand_ncol

### fitsh_cy.pyx
- `if self._fth > 0.0: th = 0.0` 存在（尚未删除）
- 方法名为 `photometer`, `subtracted_photometer`, `magnitude_fit`
- `cand_ptr, nc, self._cand_rad` 无 nc_cols
- 孔径分隔符为 `;`

### fistar/star-cand-lnk.c
- `wc->flags=0` **已存在**（session 1353 中加的修复）

### fiphot/fiphot_core.c
- 子像素数据直接透传（无 normalize+invert）
- goto 标签 `cleanup_sg` 错误引用
