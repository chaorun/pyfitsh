# 快照恢复: fitsh_cython_20260616T014204Z.zip

## 来源
- 构建于 session-ses_1347.md (6/16 01:42 UTC)
- 包含今日所有修复

## 状态总结

| 模块 | 状态 |
|------|------|
| fistar | ✓ (parabolapeak candidates 通过，fit bg/amp ~1% diff) |
| fiphot | ✓ (multiap 通过，subpixel/subtracted/magfit 未测试) |
| grmatch | ✓ (nhit=56 匹配) |
| fitrans | ✓ (bilinear max_diff=0.002) |
| ficonv | ✗ (ref_conv 通过，sub 不匹配 max_diff=46400) |

## 重建方法
以 115323Z 为基线，应用以下修改:

### setup.py
```
extra_compile_args=["-O0", "-g"]  →  extra_compile_args=["-O3"]
```

### fistar/fistar_core.c
1. sr 作用域: `} else switch (algorithm) {` → `} else { range srcrange, *sr = NULL; switch (algorithm) {`
   对应添加 `}` 封闭 else 块
2. 迭代参数: `sfp.iter_symmetric = 4;` → `= iter_symmetric;` (同理 iter_general)
3. 删除 switch 内死代码行的 iter 覆盖
4. cand_ncol 参数声明和候选读取循环
5. convert_candidates 回退移到结果提取之前，删除 `&& is_fit_model`

### fistar/fistar_core.h
- `double *cand_xy, int in_ncand` → `..., int cand_ncol,`

### fistar/fistar_cdef.pxd
- 同上

### fitsh_cy.pyx
1. 删除 `if self._fth > 0.0: th = 0.0`
2. nc_cols 支持
3. 方法名: photometer→photometry, subtracted_photometer→subtracted_photometry, magnitude_fit→magfit
4. 孔径分隔符 `;` → `,`

### fiphot/fiphot_core.c
1. 两个 flat wrapper 中添加子像素 normalize+invert
2. goto cleanup_sg → cleanup_sg2

### fistar/fistar_core.c (debug 残留)
- `fprintf(stderr,"ALG_LNK threshold=...")` 调试行待清理

### README.md / USAGE.md
- 新建，记录已知问题
